// This function is build to help auto click the Capture button when the pirate fortress is done
const autoClickCaptureButton = () => {
  const captureButton = document.querySelector('#capture-button');
  console.log('Auto-clicking capture button:', captureButton);
};
